language: generic
sudo: required
services:
  - docker
before_install:
  - docker build -t VNuserX/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true VNuserX/docker-react npm run test -- --coverage
