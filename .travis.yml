sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t v/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true v/docker-react npm run test -- --coverage
